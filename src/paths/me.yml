get:
  responses:
    '200':
      description: A JSON array of User model
      content:
        application/json: # レスポンスの形式指定
          schema:
            type: object
            items:
              $ref: '#/components/schemas/Me' # 参照するモデル
            example:
              name: kaave
              birthday: 1982-09-24
              gender: male
              height: 181
              weight: 68
